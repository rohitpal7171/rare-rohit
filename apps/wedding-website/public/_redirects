# Static files — serve directly, never redirect to index.html
/audio/*   /audio/:splat   200
/favicon.svg   /favicon.svg   200

# SPA fallback — all other routes go to index.html
/*    /index.html   200
